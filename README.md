# React proof for Misha

Короткий одностраничный пруф: визуал, UX/UI прямо в React и одна идея синхронизации сайта с LLM-контекстом. Текущая страница — восстановленная старая версия; согласованные правки перечислены в [PLAN.md](PLAN.md).

## Локальный запуск

```bash
npm ci
npm run dev
```

Рабочая ветка: `work/visual-proof`. Исходная восстановленная версия сохранена в `main` (`3febc31`). Предыдущие варианты лежат в `recovery/`.
