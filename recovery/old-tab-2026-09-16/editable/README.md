# Editable recovery candidate

This `page.tsx` was reconstructed from the preserved server-rendered HTML of the open Chrome tab at `127.0.0.1:3000/?review=5`. It is not claimed to be byte-for-byte identical to the lost source file. `globals.css` is the recovered, compiled stylesheet from Chrome's cache. The existing `WorkCatalogue` and `BriefComposer` components in the project are the same components used by that page.

The exact static browser snapshot is in the parent directory and can be previewed at `http://127.0.0.1:3002/` while its local server is running. No current application source file has been overwritten by this recovery candidate.
